require("./newpage");
const {
  calculateSum,
  calculateSub,
  calculateMultiply,
  calculateDiv,
} = require("./calculate");

calculateSum(1, 2);
calculateSub(112, 3);
calculateMultiply(3, 3);
calculateDiv(12, 3);
