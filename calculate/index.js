const { calculateSum } = require("./sum");
const { calculateSub } = require("./sub");
const { calculateMultiply } = require("./multiply");
const { calculateDiv } = require("./div");

module.exports = {
  calculateSum,
  calculateSub,
  calculateMultiply,
  calculateDiv,
};
